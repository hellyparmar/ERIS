title = "ERIS Gitleaks Config"

[allowlist]
  description = "Allowlisted test fixtures — these are deliberately fake values in unit tests"
  paths = [
    "backend/tests/unit/test_integrations.py",
    "backend/tests/unit/test_canonical_models.py",
    "backend/tests/unit/test_security.py",
    "backend/tests/conftest.py",
    ".env.example",
    "backend/.env.example",
    "frontend/.env.example",
  ]
